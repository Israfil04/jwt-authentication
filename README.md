Продвинутая JWT Аутентификация (Node.js REST API)

📖 Подробное описание проекта

Этот проект представляет собой готовый к продакшену шаблон (boilerplate) для серверной части веб-приложения, реализующий полный цикл аутентификации и авторизации пользователей.

В отличие от простых реализаций JWT, где токены хранятся в localStorage (что уязвимо для XSS-атак), данный проект использует гибридную архитектуру безопасности:
 -Access Token (короткоживущий) отправляется клиенту в теле ответа и хранится в памяти приложения.
 -Refresh Token (долгоживущий) сохраняется в защищенной HttpOnly Cookie, недоступной для JavaScript, что обеспечивает надежную защиту от кражи сессии.
 -Refresh Token также дублируется в базе данных MongoDB, что позволяет администратору принудительно разлогинить пользователя (отозвать токен).
Архитектура приложения построена на паттерне MVC (Model-View-Controller) с выделением бизнес-логики в Сервисный слой (Services), что делает код чистым, тестируемым и расширяемым.

Ключевые возможности
-Регистрация и Вход: Безопасное создание аккаунта с хешированием паролей (bcrypt).
-Двухфакторная архитектура токенов: Автоматическое обновление Access токена через Refresh токен без участия пользователя.
-Подтверждение Email: Отправка писем со ссылками активации через SMTP (Nodemailer).
-Валидация данных: Строгая проверка входящих данных (email, длина пароля) с помощью express-validator.
-DTO (Data Transfer Objects): Фильтрация данных перед отправкой на клиент (скрытие паролей и служебных полей).
-Централизованная обработка ошибок: Кастомный ApiError и middleware перехватывают все исключения, возвращая клиенту понятные сообщения.
-Защита маршрутов: Middleware авторизации для закрытия доступа к приватным эндпоинтам.

1. Клонирование и установка

git clone [https://github.com/Israfil04/jwt-authentication.git](https://github.com/Israfil04/jwt-authentication.git)
cd jwt-authentication
npm install

2. Настройка окружения (.env)

Создайте файл .env в корне проекта и заполните его своими данными:
PORT=5000
DB_URL=mongodb://localhost:27017/auth_db
API_URL=http://localhost:5000
# Секретные ключи для токенов (придумайте сложные строки)
JWT_ACCESS_SECRET=secret-key-access-123
JWT_REFRESH_SECRET=secret-key-refresh-456
# Настройки SMTP для отправки писем (например, Google Mail App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

3. Запуск сервера

Режим разработки (с авто-перезагрузкой):

npm run dev


Продакшен режим:

npm start

Автор Zakriev Israfil
